import {Vector} from "./vector";
import {Board} from "./board";

export const directionMap = {
    left: new Vector(-1, 0),
    up: new Vector(0, -1),
    right: new Vector(1, 0),
    down: new Vector(0, 1),
};
